module.exports = function() {
    var P = {};
    /*
        {
        "daily" : [
         "sunday" : {
             numberOfCommits : INT,
             numberOffHoursCommits : INT,
             numberOfcards : INT,
             numberOfAuthors : INT
          } ]
        }
    */

    P.createPunchcard = function(logObjects) {
        var days = ["sunday", "monday", "tuesday", "wednesday"];
        for(var i=; i<logObjects.length; i++)
    };

    P.

    return P
};